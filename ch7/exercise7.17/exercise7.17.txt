What, if any, are the differences between using class or struct?

The only difference between using class and using structato define a class is
the default access level. The default access level by using class is private,
while the default access level by using struct is public.
