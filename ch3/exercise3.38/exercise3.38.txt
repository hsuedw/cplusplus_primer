In this section, we noted that it was not only illegal but meaningless to try to add two pointers. Why would adding two pointers be meaningless?

A pointer is a variable whose value is the memory address of another object. Adding two pointers means add two memory addresses. It is obvious that adding two memory addresses is meaningless.
