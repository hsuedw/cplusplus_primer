#include <map>
#include <string>

using std::map;
using std::string;

int main()
{
    map<string, size_t> word_count;

    map<string, size_t>::iterator map_it = word_count.begin();

    return 0;
}
